enum class TokenType {
    NATIVE,
    PYTHON,
    KOTLIN,
    CPP,
    LBRACE,
    RBRACE,
    NATIVE_CODE,

    ID,
    EOF,

    INT,
    FLOAT,
    STRING,
    BOOL,
    LIST,
    MAP,
    SET,
    NULL,
    VAR,

    INT_CONST,
    FLOAT_CONST,
    STRING_CONST,
    BOOL_CONST,
    STRING_HEAD,
    STRING_MID,
    STRING_TAIL,

    PLUS,
    MINUS,
    MUL,
    FLOAT_DIV,
    INT_DIV,
    MODULO,
    POW,
    BITWISE_NOT,
    BITWISE_AND,
    BITWISE_OR,
    BITWISE_XOR,
    LEFT_SHIFT,
    RIGHT_SHIFT,

    ASSIGN,
    PLUS_ASSIGN,
    MINUS_ASSIGN,
    MUL_ASSIGN,
    FLOAT_DIV_ASSIGN,
    INT_DIV_ASSIGN,
    MODULO_ASSIGN,
    POW_ASSIGN,
    BITWISE_AND_ASSIGN,
    BITWISE_OR_ASSIGN,
    BITWISE_XOR_ASSIGN,
    LEFT_SHIFT_ASSIGN,
    RIGHT_SHIFT_ASSIGN,

    EQ,
    NOT_EQ,
    LT,
    GT,
    GTE,
    LTE,

    LPAREN,
    RPAREN,
    RBRACKET,
    LBRACKET,
    DOT,
    COLON,
    COMMA,
    SEMI,
    QUESTION,

    OR,
    AND,
    NOT,

    EXE,
    PRINT,
    PRINTLN,
    INPUT,
    IF,
    ELIF,
    ELSE,
    FOR,
    IN,
    FROM,
    TO,
    DOWN_TO,
    WHILE,
    BREAK,
    CONTINUE,
    CLASS,
    FUN,
    RETURN
}